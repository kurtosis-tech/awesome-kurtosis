name: "github.com/kurtosis-tech/awesome-kurtosis/redis-voting-app"
description: |
  Voting App
  ==========
  This is a toy voting application for demonstrating Kurtosis functionality.

  It accepts one configuration parameter, `should_party`, which determines whether a small party should be thrown before launching the app ;)
